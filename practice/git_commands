copy a project from github to local : git clone <url>
        example for this : git clone https://github.com/ssoni1603/java-practice.git


to get changes : git pull


git status
to add specific file: git add <<filename>>
to add all files: git add .
to remove an added file: git reset head <<file relative path>>

to commit: git commit -m "<<message>>"

git push

to add and commit at the same time: git commit -am "<<message">>

to view commit msg: git log
to view commit msgs in short: git log --oneline